# PLACEHOLDER — instance-level map for the supplied RF-DETR model.
#
# Populate after inspecting the real model object and checkpoint.
#
# Expected future contents:
#
# model:
#   id: rfdetr_baseline
#   root_aoi: RF_DETR
#
# nodes:
#   - source_path: model.encoder.layers.0.self_attn
#     canonical_id: encoder.0.attention
#     aoi: MULTIHEAD_ATTENTION
#     parameter_ids:
#       - ...
#
# Each source object used in the canonical architecture should be traceable here.

model:
  id: rfdetr_placeholder
  root_aoi: RF_DETR

nodes: []
