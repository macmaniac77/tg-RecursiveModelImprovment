"""PLACEHOLDER ONLY.

Replace or wrap this file with the user's real Tinygrad RF-DETR train.py.

Required future contract should expose, directly or through an adapter:

    build_model(...)
    build_dataloaders(...)
    build_optimizer(...)
    train_step(...)
    evaluate(...)
    save_checkpoint(...)
    load_checkpoint(...)

The original supplied training route should remain runnable as a control.

Do not invent a simplified training loop and call it RF-DETR.
"""


def _placeholder():
    raise NotImplementedError(
        "Waiting for the real Tinygrad RF-DETR train.py from the user. "
        "Read AGENT_HANDOFF.md before replacing this placeholder."
    )


if __name__ == "__main__":
    _placeholder()
